[Service]
# Production hardening — cap blast radius on the shared box (H3).
MemoryMax=768M
MemoryHigh=640M
LimitNOFILE=65535
# Filesystem hardening (does not affect app behaviour; app writes to /var/data)
NoNewPrivileges=true
PrivateTmp=true
