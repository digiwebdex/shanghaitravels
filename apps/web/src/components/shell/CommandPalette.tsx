/**
 * Cmd/Ctrl-K global search — delegates to enterprise GlobalSmartSearch (V4.4).
 * Kept as a stable import path for AdminLayout and module shells.
 */
export { GlobalSmartSearch as CommandPalette } from "@/components/search/GlobalSmartSearch";
